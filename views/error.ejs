<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>splatbin</title>
    <link rel="icon" type="image/png" href="/logo.png">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 40px 20px;
            background: #000000;
            color: #cba6f7;
            min-height: 100vh;
        }
        .container {
            background: #111111;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.8);
            text-align: center;
            border: 1px solid #333333;
        }
        .error-icon {
            width: 64px;
            height: 64px;
            margin-bottom: 20px;
            object-fit: contain;
        }
        h1 {
            color: #cba6f7;
            margin-bottom: 20px;
            font-weight: 600;
        }
        .error-message {
            font-size: 18px;
            color: #cba6f7;
            margin-bottom: 30px;
        }
        .home-link a {
            background: #cba6f7;
            color: #000000;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.2s ease;
        }
        .home-link a:hover {
            background: #f5c2e7;
            transform: translateY(-1px);
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="/logo.png" alt="splatbin" class="error-icon" width="64" height="64">
        <h1>Oops!</h1>
        <div class="error-message"><%= message %></div>
        <div class="home-link">
            <a href="/">← Go Home</a>
        </div>
    </div>
</body>
</html>
